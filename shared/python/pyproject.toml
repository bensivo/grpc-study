[project]
name = "grpcgreetings"
version = "v1"
description = "Generate GRPC libraries"

[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]